import styled from 'styled-components';

function CompletionPage() {
  
  return <CompletionWrapper>CompletionPage</CompletionWrapper>;
}

const CompletionWrapper = styled.div`
  background: aqua;
  padding: 4em;
`;

export default CompletionPage;

